<p class="card-text" style="font-size:1em; color: black;">
    {{id}}. {{burger_name}} 
    <button class="change-devour" data-id="{{id}}" data-devour="{{devour}}">
		{{!-- {{#if devoured}}Consume{{else}}TBD{{/if}} --}}
        Consume!
	</button>
</p>